# Public URL of the Python conversion backend (see backend/README.md).
# This is exposed to the browser: the frontend uploads audio directly to it
# to avoid Vercel's serverless function request body size limit.
NEXT_PUBLIC_ML_BACKEND_URL=http://localhost:8000
